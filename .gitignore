# Secrets — never commit
.env
.env.*
!.env.example

# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code skill downloads (devs install via `claude skill add`)
.claude/skills/

# Evaluation data (contains user-specific device names, MACs, IPs)
evaluation.xml

# Session artifacts (internal Claude Code dev state)
session-backup/
webfetch-audit/

# Internal planning docs (superseded by source + README)
docs/install.md
docs/phase-1-tool-list.md
firewalla-resources.txt
